The program cannot handle all images for this imput. The reason is that after SIFT, there are too many feacher points detected(around 3000-4000), so the runtime is long. After 3 images, the program will show a runtime error.